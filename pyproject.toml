[project]
name = "botarena-elo-tracking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datasets>=3.5.0",
    "huggingface-hub>=0.30.1",
]
